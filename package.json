{
  "name": "kantan-task",
  "version": "1.0.0",
  "description": "かんたんタスク：タスク管理アプリ",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env src/server.js",
    "start": "node src/server.js",
    "test": "node --env-file-if-exists=.env.test --test"
  },
  "dependencies": {
    "express": "5.2.1",
    "pg": "8.23.0"
  },
  "devDependencies": {
    "supertest": "7.2.2"
  }
}
