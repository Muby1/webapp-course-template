# テスト用の環境変数（第11章）
# このファイルをコピーして .env.test を作り、値を書き換えて使う（.env.test はGitの管理対象外）
#
# copy .env.test.example .env.test

# NeonのPostgreSQL接続文字列（第9章）。開発用プロジェクト内の、本番用とは別のDBを指定する
DATABASE_URL=ここにNeonのテスト用接続文字列を貼り付け

# テスト実行時であることを示す値。npm test はこの値が test でないと中止する（安全装置）
APP_ENV=test
